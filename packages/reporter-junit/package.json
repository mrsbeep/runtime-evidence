{
  "name": "@runtime-evidence/reporter-junit",
  "version": "0.0.0",
  "private": true,
  "description": "JUnit evidence reporter for runtime-evidence",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json --noEmit && node --test test/reporter-junit.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@runtime-evidence/evidence-schema": "0.0.0"
  }
}
