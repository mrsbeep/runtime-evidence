{
  "name": "@runtime-evidence/config",
  "version": "0.0.0",
  "private": true,
  "description": "Configuration loading and validation for runtime-evidence",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
