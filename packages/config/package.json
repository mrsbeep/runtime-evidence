{
  "name": "@runtime-evidence/config",
  "version": "0.0.0",
  "private": true,
  "description": "Configuration loading and validation for runtime-evidence",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json --noEmit && node --test test/config.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@runtime-evidence/evidence-schema": "0.0.0",
    "typebox": "1.3.19",
    "yaml": "2.9.0"
  }
}
