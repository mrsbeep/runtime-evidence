{
  "name": "@runtime-evidence/replay-http",
  "version": "0.0.0",
  "private": true,
  "description": "Policy-controlled HTTP replay for runtime-evidence",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json --noEmit && node --test test/replay-http.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@runtime-evidence/evidence-schema": "0.0.0"
  }
}
