# Packages

The initial TypeScript monorepo is split by responsibility. Package manifests and APIs will be introduced with their implementations so package boundaries can be reviewed before publication.
