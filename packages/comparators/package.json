{
  "name": "@runtime-evidence/comparators",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic observation comparators for runtime-evidence",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
